import { AuthentificationOK } from './authentification-ok';

describe('AuthentificationOK', () => {
  it('should create an instance', () => {
    expect(new AuthentificationOK()).toBeTruthy();
  });
});
